import { DefaultTheme } from "styled-components";

const customTheme: DefaultTheme = {
  colors: {
    primary: "#66defb",
    background: "#f1f1f1",
    text: "#616161",
  },
} as const;
export { customTheme };
